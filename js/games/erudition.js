// Эрудиция: «Верю — не верю», «Хронология», «Кто лишний».
(function () {
  'use strict';
  const BT = window.BT;
  const D = (BT.DATA = BT.DATA || {});
  const pick = BT.rand.pick, shuffle = BT.rand.shuffle;
  const cap = (s) => s.charAt(0).toUpperCase() + s.slice(1);

  // [утверждение, правда?, пояснение]
  const FACTS = [
    ['Страусы прячут голову в песок при опасности.', false, 'Это миф: страус пригибается к земле или убегает со скоростью до 70 км/ч.'],
    ['Молния никогда не бьёт в одно место дважды.', false, 'Бьёт, и часто: в высокие здания — десятки раз в год.'],
    ['Великую Китайскую стену видно с Луны невооружённым глазом.', false, 'Стена слишком узкая — с Луны её не разглядеть.'],
    ['Золотые рыбки помнят всего 3 секунды.', false, 'Опыты показывают, что они помнят месяцами и поддаются дрессировке.'],
    ['У осьминога три сердца.', true, 'Два качают кровь через жабры, третье — по всему телу.'],
    ['Бананы растут на деревьях.', false, 'Банан — гигантская трава: «ствол» образован свёрнутыми листьями.'],
    ['Помидор с точки зрения ботаники — ягода.', true, 'Сочный многосемянный плод — ботаническая ягода.'],
    ['Клубника с точки зрения ботаники — ягода.', false, 'Это «ложная ягода»: настоящие плоды — маленькие орешки на поверхности.'],
    ['Летучие мыши слепые.', false, 'Они видят, а в темноте дополнительно ориентируются эхолокацией.'],
    ['Эверест — самая высокая гора, если считать от подножия.', false, 'От подножия выше гавайский вулкан Мауна-Кеа, основание которого на дне океана. Эверест — выше всех над уровнем моря.'],
    ['Сутки на Венере длиннее её года.', true, 'Оборот вокруг оси — 243 земных суток, вокруг Солнца — около 225.'],
    ['Звук распространяется в открытом космосе.', false, 'В вакууме нет среды, которая передавала бы звуковые волны.'],
    ['Человек использует только 10% мозга.', false, 'Миф: снимки мозга показывают, что работают все его отделы.'],
    ['Волосы и ногти продолжают расти после смерти.', false, 'Кожа высыхает и стягивается — это создаёт иллюзию роста.'],
    ['Быки приходят в ярость именно от красного цвета.', false, 'Быки плохо различают красный — их раздражает движение плаща.'],
    ['Хамелеоны меняют цвет в основном ради маскировки.', false, 'Чаще — чтобы общаться с сородичами и регулировать температуру тела.'],
    ['Кровь в венах синего цвета.', false, 'Она тёмно-красная, а синей вены кажутся из-за того, как кожа пропускает свет.'],
    ['Наполеон был необычайно низкого роста.', false, 'Около 168–170 см — средний рост для его эпохи. Миф родился из разницы французских и английских мер.'],
    ['Викинги носили рогатые шлемы.', false, 'Рогатые шлемы придумали художники и постановщики опер XIX века.'],
    ['Эйнштейн плохо учился по математике.', false, 'Миф: математику он знал блестяще с юности.'],
    ['Колумб доказал, что Земля круглая.', false, 'Образованные европейцы знали о шарообразности Земли ещё со времён Античности.'],
    ['Мёд может храниться тысячи лет, не портясь.', true, 'Съедобный мёд находили в древнеегипетских гробницах.'],
    ['Арахис — это орех.', false, 'Арахис — бобовое растение, родственник гороха и фасоли.'],
    ['Желудок коровы состоит из четырёх отделов.', true, 'Рубец, сетка, книжка и сычуг.'],
    ['Пингвины живут на Северном полюсе.', false, 'Пингвины живут только в Южном полушарии.'],
    ['Акулы — млекопитающие.', false, 'Акулы — хрящевые рыбы.'],
    ['Киты — рыбы.', false, 'Киты — млекопитающие: дышат воздухом и выкармливают детёнышей молоком.'],
    ['Паук — насекомое.', false, 'Паук — паукообразное: у него 8 ног, а у насекомых 6.'],
    ['Верблюд хранит воду в горбах.', false, 'В горбах запас жира; воду верблюд экономит за счёт особенностей организма.'],
    ['Кошки видят в полной темноте.', false, 'Им нужно хоть немного света, но в сумерках они видят в разы лучше человека.'],
    ['Собаки видят мир только чёрно-белым.', false, 'Собаки различают синий и жёлтый, но плохо — красный и зелёный.'],
    ['Луна светит собственным светом.', false, 'Луна отражает солнечный свет.'],
    ['Чистая дистиллированная вода плохо проводит электричество.', true, 'Ток проводят растворённые в воде соли, а в чистой воде их почти нет.'],
    ['Алмаз и графит состоят из одного и того же элемента.', true, 'Оба — чистый углерод, отличается лишь строение кристаллов.'],
    ['Старые окна толще внизу, потому что стекло медленно стекает.', false, 'Стекло твёрдое; старые стёкла просто были неровными и их ставили толстым краем вниз.'],
    ['Эйфелева башня летом становится выше.', true, 'Металл расширяется от жары — башня «подрастает» примерно на 10–15 см.'],
    ['Мозг человека чувствует боль.', false, 'В ткани мозга нет болевых рецепторов; головная боль возникает в оболочках и сосудах.'],
    ['Сердце человека расположено строго слева.', false, 'Оно почти посередине грудной клетки, лишь со смещением влево.'],
    ['У однояйцевых близнецов одинаковые отпечатки пальцев.', false, 'Узор формируется в утробе под влиянием множества факторов — у близнецов он разный.'],
    ['Глубина Марианской впадины — больше 10 километров.', true, 'Около 11 км — это самая глубокая точка Мирового океана.'],
    ['Амазонка впадает в Тихий океан.', false, 'Амазонка впадает в Атлантический океан.'],
    ['Австралия — одновременно и страна, и материк.', true, 'Это единственное государство, занимающее целый материк.'],
    ['В Антарктиде есть действующие вулканы.', true, 'Например, вулкан Эребус с лавовым озером в кратере.'],
    ['Сахара — самая большая пустыня на Земле.', false, 'Самая большая — Антарктида (холодная пустыня). Сахара — крупнейшая жаркая.'],
    ['Москва старше Санкт-Петербурга.', true, 'Москва упоминается в 1147 году, Петербург основан в 1703-м.'],
    ['В Байкале около пятой части мировых запасов пресной поверхностной воды.', true, 'Это самое глубокое и самое древнее озеро на планете.'],
    ['Каспийское море с точки зрения географии — озеро.', true, 'Это крупнейшее на Земле бессточное озеро.'],
    ['Россия граничит с Японией по суше.', false, 'У России и Японии только морская граница.'],
    ['Рост Петра I был больше двух метров.', true, 'Около 2 метров 3 сантиметров.'],
    ['Шекспир и Сервантес умерли в один и тот же год.', true, 'Оба — в 1616 году.'],
    ['Прадед Пушкина, Абрам Ганнибал, был родом из Африки.', true, 'Его ребёнком привезли в Россию, и он стал крестником Петра I.'],
    ['«Войну и мир» Лев Толстой написал за один год.', false, 'Работа над романом заняла около шести лет — с 1863 по 1869 год.'],
    ['Бетховен написал Девятую симфонию, будучи почти полностью глухим.', true, 'На премьере ему пришлось повернуться к залу, чтобы увидеть аплодисменты.'],
    ['Сальери отравил Моцарта.', false, 'Это легенда, популярная благодаря Пушкину. Доказательств нет, они были коллегами.'],
    ['«Мона Лиза» хранится в Эрмитаже.', false, 'Она находится в Лувре, в Париже.'],
    ['Ван Гог при жизни продал очень мало картин.', true, 'Документально известны лишь единичные продажи — признание пришло после смерти.'],
    ['Олимпийские игры в Древней Греции проводились раз в 4 года.', true, 'Этот период так и назывался — олимпиада.'],
    ['Классический футбольный мяч сшит из 32 панелей.', true, '12 пятиугольников и 20 шестиугольников.'],
    ['Шахматы изобрели в Древнем Египте.', false, 'Предок шахмат — индийская игра чатуранга.'],
    ['Первым человеком в космосе была женщина.', false, 'Первым был Юрий Гагарин (1961), первой женщиной — Валентина Терешкова (1963).'],
    ['В русском языке есть слова, начинающиеся на букву «Ы».', true, 'Например, «ыканье» и якутские названия вроде Ыныкчан.'],
    ['Колибри может летать задом наперёд.', true, 'Это единственные птицы, способные на полноценный полёт назад.'],
    ['Муравьи никогда не спят.', false, 'У муравьёв есть регулярные периоды отдыха, похожие на сон.'],
    ['От Земли до Солнца около 150 миллионов километров.', true, 'Это расстояние называют астрономической единицей.'],
    ['Бумажные деньги впервые появились в Китае.', true, 'Ещё в VII–XI веках, задолго до Европы.'],
    ['Кофейные «зёрна» — это семена плодов кофейного дерева.', true, 'Плод кофе похож на вишню, а «зёрна» — его косточки.'],
    ['Ананасы растут на пальмах.', false, 'Ананас растёт на невысоком травянистом растении у самой земли.'],
    ['Картофель — родственник помидора.', true, 'Оба относятся к семейству паслёновых.'],
    ['Крупные попугаи могут жить дольше 50 лет.', true, 'Ара и какаду нередко доживают до 60–80 лет.'],
    ['Кенгуру практически не умеют ходить назад.', true, 'Мешают строение задних ног и массивный хвост.'],
    ['Больше всего кислорода на Земле вырабатывают леса.', false, 'Около половины и больше дают океанские водоросли и фитопланктон.'],
    ['Помидоры, картофель и баклажаны — ядовитые растения, если есть их зелёные части.', true, 'В ботве и зелёных плодах паслёновых есть ядовитый соланин.'],
    ['Солнце — жёлтая звезда.', false, 'Солнечный свет почти белый; жёлтым Солнце кажется из-за рассеяния в атмосфере.'],
    ['На Эвересте кипящая вода холоднее, чем на уровне моря.', true, 'Из-за низкого давления вода там кипит примерно при 70 °C.'],
    ['Самый большой орган человека — печень.', false, 'Самый большой орган — кожа.'],
  ];

  BT.registerGame({
    id: 'truefalse', cat: 'words', icon: 'truefalse',
    name: 'Верю — не верю', short: 'Факт или миф? С пояснениями',
    howto: [
      'Прочитайте утверждение и решите: правда это или миф.',
      'После ответа — короткое пояснение. Так каждая игра прибавляет знаний.',
      '12 утверждений, на каждое 15 секунд. Быстрый верный ответ приносит больше очков.',
    ],
    ref: 900, levels: false,
    run(ctx) {
      const h = ctx.h;
      const seen = BT.store.seen(ctx.me.id, 'facts');
      let pool = FACTS.map((f, i) => i).filter((i) => !seen.has(i));
      if (pool.length < 12) {
        BT.store.resetSeen(ctx.me.id, 'facts');
        pool = FACTS.map((f, i) => i);
      }
      const qs = shuffle(pool).slice(0, 12);
      const PER = 15, review = [];
      let k = -1, score = 0, right = 0, timer = null, locked = true;
      const counter = h('div', { class: 'quiz-topic' });
      const card = h('div', { class: 'tf-card' });
      const yes = h('button', { type: 'button', class: 'tf-btn yes', text: 'Верю' });
      const no = h('button', { type: 'button', class: 'tf-btn no', text: 'Не верю' });
      const note = h('div', { class: 'tf-note hidden' });
      const nextBtn = h('button', { type: 'button', class: 'btn hidden', text: 'Дальше' });
      ctx.stage.append(counter, card, h('div', { class: 'tf-btns' }, yes, no), note, nextBtn);
      ctx.hud({ score: 0 });
      ctx.tap(yes, () => answer(true, yes));
      ctx.tap(no, () => answer(false, no));
      ctx.tap(nextBtn, () => next());

      function next() {
        k++;
        if (k >= qs.length) return finish();
        const f = FACTS[qs[k]];
        counter.textContent = 'Утверждение ' + (k + 1) + ' из ' + qs.length;
        card.textContent = f[0];
        [yes, no].forEach((b) => { b.className = b.className.replace(/ (right|wrong|dim)/g, ''); });
        note.classList.add('hidden');
        nextBtn.classList.add('hidden');
        locked = false;
        timer = ctx.timer(PER, () => answer(null, null));
      }
      function answer(v, b) {
        if (locked) return;
        locked = true;
        const left = timer.left();
        timer.stop();
        const f = FACTS[qs[k]];
        const ok = v === f[1];
        const rightBtn = f[1] ? yes : no;
        rightBtn.classList.add('right');
        if (b && !ok) b.classList.add('wrong');
        if (!b) [yes, no].forEach((x) => { if (x !== rightBtn) x.classList.add('dim'); });
        if (ok) {
          right++;
          const pts = 60 + Math.round((40 * left) / (PER * 1000));
          score += pts;
          ctx.hud({ score });
          ctx.good(b, '+' + pts);
        } else {
          ctx.bad(b);
        }
        note.innerHTML = '';
        note.append(h('b', { text: f[1] ? 'Правда. ' : 'Миф. ' }), f[2]);
        note.classList.remove('hidden');
        nextBtn.textContent = k + 1 < qs.length ? 'Дальше' : 'Результат';
        nextBtn.classList.remove('hidden');
        review.push({ q: f[0], your: v == null ? 'время вышло' : v ? 'Верю' : 'Не верю', right: f[1] ? 'Правда' : 'Миф', ok, note: f[2] });
      }
      function finish() {
        BT.store.markSeen(ctx.me.id, 'facts', qs);
        ctx.end({
          score, accuracy: right / qs.length, review, reviewTitle: 'Факты и мифы',
          stats: [['Верно', right + ' из ' + qs.length], ['Мифов было', qs.filter((i) => !FACTS[i][1]).length], ['Очков', score]],
          details: { right },
        });
      }
      next();
    },
  });

  // ------------------------------------------------------------------ Хронология
  const GAPS = [null, 40, 15, 6, 2];
  BT.registerGame({
    id: 'chrono', cat: 'words', icon: 'chrono',
    name: 'Хронология', short: 'Расставь события по порядку',
    howto: [
      'Четыре исторических события — расставьте их от самого раннего к самому позднему.',
      'Нажимайте по порядку; повторное нажатие снимает номер.',
      '8 раундов. После каждого — годы всех событий. Чем выше уровень, тем ближе даты.',
    ],
    ref: 700, startLevel: 1, maxLevel: 4,
    run(ctx) {
      const h = ctx.h;
      const lvl = BT.clamp(ctx.level, 1, 4);
      const DATES = (D.sets && D.sets.dates) || [];
      const ROUNDS = 8, review = [];
      let round = 0, score = 0, perfect = 0, totalRight = 0, order = [], items = [], locked = true;
      const msg = h('div', { class: 'stage-msg strong' });
      const list = h('div', { class: 'chrono-list' });
      const checkBtn = h('button', { type: 'button', class: 'btn hidden', text: 'Дальше' });
      ctx.stage.append(msg, list, checkBtn);
      ctx.hud({ score: 0, progress: 0 });
      ctx.tap(checkBtn, () => next());

      function pickEvents() {
        for (let tries = 0; tries < 200; tries++) {
          const ev = BT.rand.sample(DATES, 4);
          const ys = ev.map((e) => e[1]).sort((a, b) => a - b);
          if (ys.every((y, i) => i === 0 || y - ys[i - 1] >= GAPS[lvl])) return ev;
        }
        return BT.rand.sample(DATES, 4);
      }
      function next() {
        round++;
        if (round > ROUNDS) return finish();
        ctx.hud({ text: round + '/' + ROUNDS, progress: (round - 1) / ROUNDS });
        msg.textContent = 'Что было раньше? Нажимайте от раннего к позднему';
        checkBtn.classList.add('hidden');
        items = pickEvents();
        order = [];
        list.innerHTML = '';
        items.forEach((e, i) => {
          const b = h('button', { type: 'button', class: 'chrono-item' }, h('span', { class: 'n' }), h('span', { class: 't', text: cap(e[0]) }), h('span', { class: 'yr' }));
          ctx.tap(b, () => tapItem(i));
          list.append(b);
        });
        locked = false;
      }
      function draw() {
        Array.from(list.children).forEach((b, i) => {
          const pos = order.indexOf(i);
          b.querySelector('.n').textContent = pos >= 0 ? pos + 1 : '';
          b.classList.toggle('on', pos >= 0);
        });
      }
      function tapItem(i) {
        if (locked) return;
        const pos = order.indexOf(i);
        if (pos >= 0) order.splice(pos, 1);
        else order.push(i);
        BT.sound.tap();
        BT.haptic();
        draw();
        if (order.length === 4) check();
      }
      function check() {
        locked = true;
        const correct = items.map((e, i) => i).sort((a, b) => items[a][1] - items[b][1]);
        let ok = 0;
        Array.from(list.children).forEach((b, i) => {
          const good = order.indexOf(i) === correct.indexOf(i);
          if (good) ok++;
          b.classList.add(good ? 'right' : 'wrong');
          b.querySelector('.yr').textContent = items[i][1];
          b.querySelector('.n').textContent = correct.indexOf(i) + 1;
          if (!good) review.push({ q: cap(items[i][0]), your: 'место ' + (order.indexOf(i) + 1), right: items[i][1] + ' год', ok: false });
        });
        totalRight += ok;
        let pts = ok * 20;
        if (ok === 4) {
          perfect++;
          pts += 20;
          ctx.good(list, '+' + pts);
        } else {
          ctx.bad(list, '+' + pts);
        }
        score += pts;
        ctx.hud({ score });
        msg.textContent = ok === 4 ? 'Всё верно!' : 'Верно ' + ok + ' из 4 — вот правильный порядок';
        checkBtn.textContent = round < ROUNDS ? 'Дальше' : 'Результат';
        checkBtn.classList.remove('hidden');
      }
      function finish() {
        const nl = perfect >= 6 ? lvl + 1 : perfect <= 2 ? lvl - 1 : lvl;
        ctx.end({
          score, accuracy: totalRight / (ROUNDS * 4), nextLevel: BT.clamp(nl, 1, 4), review, reviewTitle: 'Даты событий',
          stats: [['Без ошибок', perfect + ' из ' + ROUNDS], ['На своих местах', totalRight + ' из ' + ROUNDS * 4], ['Уровень', lvl]],
          details: { perfect, totalRight },
        });
      }
      next();
    },
  });

  // ------------------------------------------------------------------ Кто лишний
  // Семейства групп: лишний элемент берётся из другой группы того же семейства.
  const FAMILIES = [
    [
      { one: 'млекопитающее', many: 'млекопитающие', items: ['Дельфин', 'Кит', 'Летучая мышь', 'Ёж', 'Бобр', 'Тюлень', 'Слон', 'Коала', 'Утконос', 'Морж'] },
      { one: 'птица', many: 'птицы', items: ['Пингвин', 'Страус', 'Колибри', 'Сова', 'Дятел', 'Киви', 'Орёл', 'Аист', 'Фламинго', 'Эму'] },
      { one: 'рыба', many: 'рыбы', items: ['Акула', 'Щука', 'Лосось', 'Скат', 'Карп', 'Тунец', 'Морской конёк', 'Сом', 'Треска'] },
      { one: 'насекомое', many: 'насекомые', items: ['Муравей', 'Пчела', 'Бабочка', 'Жук', 'Комар', 'Стрекоза', 'Кузнечик', 'Муха'] },
      { one: 'пресмыкающееся', many: 'пресмыкающиеся', items: ['Крокодил', 'Черепаха', 'Кобра', 'Ящерица', 'Хамелеон', 'Игуана', 'Питон', 'Варан'] },
      { one: 'паукообразное', many: 'паукообразные', items: ['Паук', 'Скорпион', 'Клещ', 'Тарантул'] },
    ],
    [
      { one: 'планета', many: 'планеты', items: ['Меркурий', 'Венера', 'Марс', 'Юпитер', 'Сатурн', 'Уран', 'Нептун', 'Земля'] },
      { one: 'звезда', many: 'звёзды', items: ['Солнце', 'Сириус', 'Вега', 'Полярная', 'Бетельгейзе', 'Альдебаран', 'Ригель'] },
      { one: 'спутник планеты', many: 'спутники планет', items: ['Луна', 'Фобос', 'Деймос', 'Титан', 'Ганимед', 'Ио', 'Каллисто', 'Европа'] },
    ],
    [
      { one: 'струнный инструмент', many: 'струнные', items: ['Скрипка', 'Виолончель', 'Гитара', 'Арфа', 'Балалайка', 'Контрабас', 'Домра', 'Альт'] },
      { one: 'духовой инструмент', many: 'духовые', items: ['Флейта', 'Труба', 'Саксофон', 'Кларнет', 'Гобой', 'Тромбон', 'Фагот', 'Туба'] },
      { one: 'ударный инструмент', many: 'ударные', items: ['Барабан', 'Литавры', 'Ксилофон', 'Бубен', 'Тарелки', 'Кастаньеты', 'Треугольник'] },
    ],
    [
      { one: 'греческий бог', many: 'греческие боги', items: ['Зевс', 'Посейдон', 'Аид', 'Афина', 'Арес', 'Афродита', 'Аполлон', 'Артемида', 'Гермес', 'Гефест'] },
      { one: 'римский бог', many: 'римские боги', items: ['Юпитер', 'Марс', 'Нептун', 'Венера', 'Минерва', 'Меркурий', 'Вулкан', 'Диана'] },
      { one: 'скандинавский бог', many: 'скандинавские боги', items: ['Один', 'Тор', 'Локи', 'Фрейя', 'Бальдр', 'Хеймдалль'] },
      { one: 'славянский бог', many: 'славянские боги', items: ['Перун', 'Велес', 'Даждьбог', 'Стрибог', 'Сварог', 'Ярило'] },
    ],
    [
      { one: 'единица длины', many: 'единицы длины', items: ['Метр', 'Дюйм', 'Миля', 'Ярд', 'Фут', 'Аршин', 'Верста'] },
      { one: 'единица массы', many: 'единицы массы', items: ['Килограмм', 'Фунт', 'Унция', 'Тонна', 'Карат', 'Пуд', 'Центнер'] },
      { one: 'единица объёма', many: 'единицы объёма', items: ['Литр', 'Галлон', 'Баррель', 'Пинта', 'Ведро', 'Кубометр'] },
    ],
    [
      { one: 'река', many: 'реки', items: ['Волга', 'Лена', 'Нил', 'Амазонка', 'Дунай', 'Енисей', 'Миссисипи', 'Янцзы', 'Обь', 'Рейн'] },
      { one: 'озеро', many: 'озёра', items: ['Байкал', 'Ладожское', 'Онежское', 'Виктория', 'Титикака', 'Женевское', 'Селигер', 'Иссык-Куль'] },
      { one: 'горная вершина', many: 'горные вершины', items: ['Эверест', 'Эльбрус', 'Монблан', 'Килиманджаро', 'Казбек', 'Аконкагуа', 'Фудзияма', 'Белуха'] },
    ],
    [
      { one: 'металл', many: 'металлы', items: ['Железо', 'Медь', 'Золото', 'Серебро', 'Алюминий', 'Цинк', 'Олово', 'Свинец', 'Никель', 'Титан'] },
      { one: 'неметалл', many: 'неметаллы', items: ['Кислород', 'Азот', 'Сера', 'Фосфор', 'Хлор', 'Йод', 'Углерод', 'Водород', 'Неон', 'Фтор'] },
    ],
  ];
  const CONT = { eu: ['страна Европы', 'страны Европы'], as: ['страна Азии', 'страны Азии'], af: ['страна Африки', 'страны Африки'], am: ['страна Америки', 'страны Америки'] };
  let allFamilies = null;
  function families() {
    if (allFamilies) return allFamilies;
    allFamilies = FAMILIES.slice();
    const S = D.sets || {};
    const people = (rows, one, many) => ({ one, many, items: Array.from(new Set(rows.map((r) => r[1]).filter((n) => !/ и |Братья/.test(n)))) });
    if (S.books && S.music && S.paintings) {
      allFamilies.push([people(S.books, 'писатель', 'писатели'), people(S.music, 'композитор', 'композиторы'), people(S.paintings, 'художник', 'художники')]);
    }
    if (S.countries) {
      allFamilies.push(Object.keys(CONT).map((c) => ({ one: CONT[c][0], many: CONT[c][1], items: S.countries.filter((r) => r[3] === c).map((r) => r[0]) })));
    }
    return allFamilies;
  }
  function makeOdd() {
    const fam = pick(families());
    const [a, b] = BT.rand.sample(fam, 2);
    const three = BT.rand.sample(a.items, 3);
    const odd = pick(b.items.filter((x) => !three.includes(x)));
    return { options: shuffle(three.concat(odd)), odd, note: '«' + odd + '» — ' + b.one + ', остальные — ' + a.many + '.' };
  }

  BT.registerGame({
    id: 'odd', cat: 'words', icon: 'odd',
    name: 'Кто лишний', short: 'Найди то, что не подходит',
    howto: [
      'Из четырёх вариантов три объединены общим признаком.',
      'Найдите лишний. Бывают ловушки: например, дельфин среди рыб.',
      '10 раундов по 15 секунд. После ответа — пояснение.',
    ],
    ref: 800, levels: false,
    run(ctx) {
      const h = ctx.h;
      const N = 10, PER = 15, review = [];
      let k = 0, score = 0, right = 0, timer = null, locked = true, cur = null;
      const msg = h('div', { class: 'stage-msg strong', text: 'Что лишнее?' });
      const grid = h('div', { class: 'choices' });
      const note = h('div', { class: 'tf-note hidden' });
      ctx.stage.append(msg, grid, note);
      ctx.hud({ score: 0 });

      function next() {
        k++;
        if (k > N) return finish();
        cur = makeOdd();
        msg.textContent = 'Что лишнее? · ' + k + ' из ' + N;
        note.classList.add('hidden');
        grid.innerHTML = '';
        cur.options.forEach((v) => {
          const b = h('button', { type: 'button', class: 'choice txt', text: v });
          ctx.tap(b, () => choose(v, b));
          grid.append(b);
        });
        locked = false;
        timer = ctx.timer(PER, () => choose(null, null));
      }
      function choose(v, b) {
        if (locked) return;
        locked = true;
        const left = timer.left();
        timer.stop();
        const ok = v === cur.odd;
        Array.from(grid.children).forEach((x) => {
          if (x.textContent === cur.odd) x.classList.add('right');
          else if (x !== b) x.classList.add('dim');
        });
        if (ok) {
          right++;
          const pts = 60 + Math.round((40 * left) / (PER * 1000));
          score += pts;
          ctx.hud({ score });
          ctx.good(b, '+' + pts);
        } else {
          if (b) b.classList.add('wrong');
          ctx.bad(b);
        }
        note.textContent = cur.note;
        note.classList.remove('hidden');
        review.push({ q: cur.options.join(', '), your: v == null ? 'время вышло' : v, right: cur.odd, ok, note: cur.note });
        ctx.clock.after(ok ? 1500 : 2600, next);
      }
      function finish() {
        ctx.end({
          score, accuracy: right / N, review, reviewTitle: 'Кто был лишним',
          stats: [['Верно', right + ' из ' + N], ['Очков', score], ['Точность', BT.fmt.pct(right / N)]],
          details: { right },
        });
      }
      next();
    },
  });
})();
